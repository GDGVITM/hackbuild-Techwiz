rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to chat files for authenticated users
    match /chat-files/{roomId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
} 